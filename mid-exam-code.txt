
git init

git config --global user.name"rajdeep1113"
git config --global user.email "rajdeepsinh7568@gmail.com"

git config --list

echo "This is read me file" > README.md

git status
git add README.md
git status

git commit -m "This is my first commit in main branch"

git status

git branch -M main
git branch

git remote add origin https://github.com/rajdeep1113/mid_exam.git
git push -u origin main


echo "HELLO" >> README.md

git add README.md
git commit -m "This is my second commit"
git push -u origin main

git checkout -b feature/login
git branch

echo "feature login" > login.md

git add login.md
git commit -m "This is feature login"

git push -u origin feature/login

git checkout main
git branch

git merge --no-ff feature/login
git push

git tag -a v1.0.1 -m "This is Version 1.0.1"
git push origin v1.0.1

echo "This is another change" >> login.md

git status
git add login.md
git commit -m "My another commit"
git push -u origin main

git checkout feature/login
git branch

git merge --no-ff main
git push